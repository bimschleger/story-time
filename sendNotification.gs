/*

Send an email notification whenever new content is added to the spreadsheet.

@param story {object} the story object

*/

function sendEmailNotification(story) {
  
  let recipient = "brian@bimschleger.com";
  let subject = "New story time: ";
  // set up gmail
  // send email to me
  // format email with simple unordered lists.
}